CMAKE_MINIMUM_REQUIRED (VERSION 2.8)

LINK_DIRECTORIES (
    ${PYTHON_LIBRARY_DIRS} ${GTKMM_LIBRARY_DIRS} ${SQLITE3_LIBRARY_DIRS} ${CAIRO_LIBRARY_DIRS})

INCLUDE_DIRECTORIES (
    ${PYTHON_INCLUDE_DIRS} ${GTKMM_INCLUDE_DIRS}  ${SQLITE3_INCLUDE_DIRS} ${CAIRO_INCLUDE_DIRS})

#ADD_DEFINITIONS (-Wall -W -g)
ADD_DEFINITIONS (-Wall -W)
SET (CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O2 -funroll-loops")

SET (SRC spikedb.cpp gui.cpp uiMenuBar.cpp uiAnalysis.cpp uiFileDetailsTreeView.cpp uiFilterFrame.cpp uiTags.cpp spikedata.cpp settings.cpp easyplotmm/easyplotmm.cpp tokenize.cpp)

ADD_EXECUTABLE (spikedb ${SRC})

TARGET_LINK_LIBRARIES (spikedb ${PYTHON_LIBRARIES} ${GTKMM_LIBRARIES} ${SQLITE3_LIBRARIES} ${CAIRO_LIBRARIES})


INSTALL(TARGETS spikedb RUNTIME DESTINATION bin)
